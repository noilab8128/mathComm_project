# 개발 작업 로그 (2026-03-18)

오늘 진행된 주요 기능 업데이트와 데이터베이스 변경 사항을 정리한 문서입니다. 다음 개발자가 작업 내용을 파악하고 이어서 개발할 수 있도록 작성되었습니다.

## 1. 주요 작업 요약
- **보안 강화**: Cloudflare Turnstile(매크로 방지 가차) 서버사이드 검증 구현.
- **큐 시스템 고도화**: 'My Queue'의 데이터베이스 영속성 구현 및 비즈니스 로직 적용.
- **상태 추적 시스템**: 문제별 '좋아요(Like)' 및 '시작함(Start)' 상태의 영속성 구현.

---

## 2. Supabase 데이터베이스 변경 사항

### 새로운 테이블 정의

#### `public.user_queue` (유저별 문제 큐)
- **용도**: 사용자가 나중에 풀기 위해 즐겨찾기한 문제 목록 저장.
- **컬럼**:
  - `user_id` (UUID, FK to auth.users): 사용자 ID.
  - `problem_id` (UUID, FK to public.problems): 문제 ID.
  - `created_at` (TIMESTAMPTZ): 추가된 시간.
- **제약 조건**: `(user_id, problem_id)` 복합 기본키 (중복 방지).

#### `public.user_likes` (좋아요)
- **용도**: 사용자가 '좋아요'를 누른 문제 추적.
- **컬럼**:
  - `user_id` (UUID, FK to auth.users): 사용자 ID.
  - `problem_id` (UUID, FK to public.problems): 문제 ID.
  - `created_at` (TIMESTAMPTZ): 좋아요 누른 시간.
- **트리거 연동**: 데이터 삽입/삭제 시 `public.problems` 테이블의 `likes_count` 컬럼이 자동으로 업데이트됩니다.

#### `public.user_starts` (시작한 문제)
- **용도**: 사용자가 실질적으로 문제를 풀기 시작했는지(Solve 버튼 클릭) 여부 추적.
- **컬럼**:
  - `user_id` (UUID, FK to auth.users): 사용자 ID.
  - `problem_id` (UUID, FK to public.problems): 문제 ID.
  - `created_at` (TIMESTAMPTZ): 시작한 시간.
- **트리거 연동**: 데이터 삽입/삭제 시 `public.problems` 테이블의 `starts_count` 컬럼이 자동으로 업데이트됩니다.

### 데이터 자동 동기화 (Triggers)
- **`sync_problem_counts` 함수**: `user_likes`와 `user_starts` 테이블의 변화를 감지하여 `problems` 테이블의 전역 카운터를 동기화합니다.

---

## 3. 주요 코드 구현 사항

### Backend (Next.js API Routes)
- **`/api/user/queue`**: 큐 목록 조회, 추가(최대 5개 제한), 삭제 처리.
- **`/api/user/like`**: 좋아요 상태 토글 및 목록 조회.
- **`/api/user/start`**: 문제 시작 시 상태 저장 및 목록 조회.

### Frontend (Hooks & UI)
- **`src/hooks/useUserInteractions.ts`**:
  - `useMyQueue`, `useLikes`, `useStarts` 커스텀 훅을 통해 데이터베이스와 UI 상태를 동기화합니다.
  - **Optimistic UI**: 사용자가 버튼을 누르는 즉시 화면이 반응하고, 서버 작업은 백그라운드에서 처리되어 사용자 경험이 쾌적합니다. (오류 발생 시 자동 롤백)
  
- **`User_home_page.tsx`**:
  - 사용자가 이미 시작한 문제는 **"Solve" 대신 파란색 "Resume"** 버튼으로 표시됩니다.
  - 사용자가 좋아요를 누른 문제는 **분홍색 하트**가 채워진 상태로 유지됩니다.
  - 큐에 이미 들어있는 문제는 추천 목록에서 자동으로 제외됩니다.

---

## 4. 향후 작업 가이드
1. **가차(Turnstile) 운영 환경 적용**: 배포 시 `.env`의 테스트 키를 운영용 키로 교체하고 Cloudflare 대시보드에서 도메인을 등록해야 합니다.
2. **카운터 동기화**: 만약 `problems` 테이블의 카운트가 실제 데이터와 맞지 않을 경우, Supabase에서 간단한 `UPDATE` 쿼리를 통해 재집계가 필요할 수 있습니다.
3. **추천 알고리즘 확장**: 현재 큐에 있는 문제를 제외하는 로직이 적용되어 있으나, 향후 '풀이 완료(Complete)'된 문제도 제외하도록 고도화할 수 있습니다.
